# @fonts/tsconfig

Shared `base.json` tsconfig extended by every package/app in the monorepo.

```json
{
  "extends": "@fonts/tsconfig/base.json"
}
```

Target ES2022, strict mode, `noUncheckedIndexedAccess`, `verbatimModuleSyntax`, bundler module resolution.
